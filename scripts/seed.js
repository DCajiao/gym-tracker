/**
 * Los datos de prueba están en database_migrations/01_seed_testing_data.sql
 * y se aplican automáticamente con: npm run db:migrate
 *
 * Este archivo existe como punto de entrada para seeds programáticos futuros
 * que no encajen bien en SQL puro (ej: datos con lógica dinámica de fechas).
 */
console.log("Los datos de prueba se aplican con: npm run db:migrate");
console.log("Ver: database_migrations/01_seed_testing_data.sql");
